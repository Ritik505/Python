A = eval(input("Enter the list of values: "))

print("Before swap",A)

for i in range(0, len(A) , 2):
    if i+1 < len(A):
        temp = A[i]
        A[i] = A[i+1]
        A[i+1] = temp
        
print("After swap",A)
